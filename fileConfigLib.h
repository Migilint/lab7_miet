#include <iostream>
#include "struct.h"

int error_open_zero_file(char[]);
int error_open(FILE*);
int error_close(FILE*);
FILE* open_file(char[], bool&);
FILE* save_file(char[], FILE*, int, prepod**);
